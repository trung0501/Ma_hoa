The program to build the Ma_Hoa function (variable chain) to encrypt k chain imported from the keyboard according to the following rule:

   't' -> 'r'
   
   'r' -> 'u'
  
   'u' -> 'n'
  
   'n' -> 'g'
 
   'g' -> 't'
